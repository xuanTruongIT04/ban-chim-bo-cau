---
phase: quick
plan: 260328-pl9
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Domain/Auth/Repositories/AdminUserRepositoryInterface.php
  - app/Infrastructure/Persistence/Repositories/EloquentAdminUserRepository.php
  - app/Application/Auth/Actions/LoginAdminAction.php
  - app/Presentation/Http/Controllers/Auth/AuthController.php
  - tests/Feature/Auth/LoginTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "Login response returns token and expires_at under a data key"
    - "createToken returns an array with token string and expires_at datetime"
    - "All existing login tests pass with updated assertions"
  artifacts:
    - path: "app/Domain/Auth/Repositories/AdminUserRepositoryInterface.php"
      provides: "Updated createToken signature returning array"
    - path: "app/Infrastructure/Persistence/Repositories/EloquentAdminUserRepository.php"
      provides: "createToken returns array with token and expires_at"
    - path: "app/Presentation/Http/Controllers/Auth/AuthController.php"
      provides: "Login response wrapped under data key"
  key_links:
    - from: "EloquentAdminUserRepository::createToken"
      to: "LoginAdminAction::handle"
      via: "return type change from string to array"
    - from: "LoginAdminAction::handle"
      to: "AuthController::login"
      via: "array destructuring for token and expires_at"
---

<objective>
Fix login response structure: change createToken() to return an array with token and expires_at, propagate the return type change through the Clean Architecture layers, and update AuthController to wrap the response under a `data` key.

Purpose: Provide clients with token expiry information in the login response and follow a consistent `data` envelope pattern.
Output: Updated auth flow returning `{ success: true, data: { token, expires_at } }`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/Domain/Auth/Repositories/AdminUserRepositoryInterface.php
@app/Infrastructure/Persistence/Repositories/EloquentAdminUserRepository.php
@app/Application/Auth/Actions/LoginAdminAction.php
@app/Presentation/Http/Controllers/Auth/AuthController.php
@tests/Feature/Auth/LoginTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update createToken to return array with token and expires_at</name>
  <files>
    app/Domain/Auth/Repositories/AdminUserRepositoryInterface.php,
    app/Infrastructure/Persistence/Repositories/EloquentAdminUserRepository.php,
    app/Application/Auth/Actions/LoginAdminAction.php
  </files>
  <action>
1. In `AdminUserRepositoryInterface.php`, change the `createToken` return type from `string` to `array` (PHPDoc: `@return array{token: string, expires_at: string}`).

2. In `EloquentAdminUserRepository.php`:
   - Compute `$expiresAt = now()->addMinutes((int) config('sanctum.expiration', 43200))` and store in a variable.
   - Create the Sanctum token with that `$expiresAt` as the `expiresAt` parameter.
   - Return an array: `['token' => $newAccessToken->plainTextToken, 'expires_at' => $expiresAt->toIso8601String()]`.

3. In `LoginAdminAction.php`, change the return type of `handle()` from `string` to `array` (PHPDoc: `@return array{token: string, expires_at: string}`). The method body stays the same — it just passes through the array from `createToken`.
  </action>
  <verify>
    <automated>cd /Users/toney/projects/ban-chim-bo-cau && php -d memory_limit=512M vendor/bin/phpstan analyse app/Domain/Auth/Repositories/AdminUserRepositoryInterface.php app/Infrastructure/Persistence/Repositories/EloquentAdminUserRepository.php app/Application/Auth/Actions/LoginAdminAction.php --level 6 --no-progress 2>&1 | tail -20</automated>
  </verify>
  <done>createToken returns array with token and expires_at, LoginAdminAction passes it through, PHPStan passes at level 6</done>
</task>

<task type="auto">
  <name>Task 2: Update AuthController response and tests</name>
  <files>
    app/Presentation/Http/Controllers/Auth/AuthController.php,
    tests/Feature/Auth/LoginTest.php
  </files>
  <action>
1. In `AuthController::login()`:
   - The `$token` variable is now an array. Rename to `$tokenData`.
   - Return: `response()->json(['success' => true, 'data' => ['token' => $tokenData['token'], 'expires_at' => $tokenData['expires_at']]], 200)`.

2. In `tests/Feature/Auth/LoginTest.php`, update the successful login test:
   - Change `assertJsonStructure(['success', 'token'])` to `assertJsonStructure(['success', 'data' => ['token', 'expires_at']])`.
   - Change `assertJsonPath('token', ...)` to `assertJsonPath('data.token', fn ($v) => is_string($v) && strlen($v) > 10)`.
   - Add `assertJsonPath('data.expires_at', fn ($v) => is_string($v) && strlen($v) > 10)` to verify expires_at is present.
   - All other tests (401, 422, Vietnamese validation) should remain unchanged as they do not reference the token structure.
  </action>
  <verify>
    <automated>cd /Users/toney/projects/ban-chim-bo-cau && php artisan test tests/Feature/Auth/LoginTest.php 2>&1 | tail -20</automated>
  </verify>
  <done>Login response returns `{ success: true, data: { token: "...", expires_at: "..." } }`, all 5 login tests pass</done>
</task>

</tasks>

<verification>
Run the full auth test suite:
```bash
php artisan test tests/Feature/Auth/ --stop-on-failure
```

Run PHPStan on all modified files:
```bash
php -d memory_limit=512M vendor/bin/phpstan analyse app/Domain/Auth/ app/Infrastructure/Persistence/Repositories/ app/Application/Auth/ app/Presentation/Http/Controllers/Auth/ --level 6
```
</verification>

<success_criteria>
- POST /api/v1/admin/login returns `{ success: true, data: { token: "...", expires_at: "2026-..." } }`
- All 5 existing login tests pass with updated assertions
- PHPStan level 6 passes on all modified files
- createToken returns array throughout the Clean Architecture layers (Interface -> Repository -> Action -> Controller)
</success_criteria>

<output>
After completion, create `.planning/quick/260328-pl9-fix-login-response-structure-change-crea/260328-pl9-SUMMARY.md`
</output>
