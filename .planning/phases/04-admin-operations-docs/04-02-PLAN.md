---
phase: 04-admin-operations-docs
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/Presentation/Http/Controllers/Auth/AuthController.php
  - app/Presentation/Http/Controllers/Admin/CategoryController.php
  - app/Presentation/Http/Controllers/Admin/ProductController.php
  - app/Presentation/Http/Controllers/Admin/StockAdjustmentController.php
  - app/Presentation/Http/Controllers/Admin/ProductImageController.php
  - app/Presentation/Http/Controllers/Admin/OrderController.php
  - app/Presentation/Http/Controllers/Admin/DashboardController.php
  - app/Presentation/Http/Controllers/Public/ProductController.php
  - app/Presentation/Http/Controllers/Public/CartController.php
  - app/Presentation/Http/Controllers/Public/CheckoutController.php
  - config/scribe.php
autonomous: false

requirements: [ADMN-04]

must_haves:
  truths:
    - "php artisan scribe:generate exits 0 and produces public/docs/index.html"
    - "Generated docs list ALL v1 endpoints grouped logically in Vietnamese"
    - "Every POST/PATCH/DELETE endpoint has @response docblock with example JSON"
    - "Public endpoints marked with @unauthenticated"
    - "All @bodyParam and @queryParam descriptions are in Vietnamese"
  artifacts:
    - path: "public/docs/index.html"
      provides: "Generated API documentation"
      min_lines: 100
    - path: "config/scribe.php"
      provides: "Scribe configuration with group ordering"
      contains: "groups"
  key_links:
    - from: "config/scribe.php"
      to: "public/docs/index.html"
      via: "php artisan scribe:generate"
      pattern: "routes.*prefixes.*api"
---

<objective>
Complete Scribe API documentation covering all v1 endpoints with Vietnamese descriptions.

Purpose: Frontend developer needs a browsable API reference with accurate request/response examples to integrate with the backend. This is the final deliverable of v1.
Output: Scribe docblock annotations on all controllers, configured group ordering, generated `public/docs/index.html` + OpenAPI spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-operations-docs/04-CONTEXT.md
@.planning/phases/04-admin-operations-docs/04-RESEARCH.md
@.planning/phases/04-admin-operations-docs/04-01-SUMMARY.md

<interfaces>
<!-- Existing Scribe @group patterns from codebase -->
Current @group values in controllers:
- AuthController: @group Auth
- CategoryController: @group Admin > Danh muc
- ProductController (Admin): @group Admin > San pham
- ProductImageController: @group Admin > Anh san pham
- StockAdjustmentController: @group Admin > Ton kho
- OrderController: @group Admin - Orders (NOTE: uses dash, not >, needs standardizing to >)
- DashboardController: @group Admin > Dashboard (from Plan 01)
- ProductController (Public): @group Public > San pham
- CartController: @group Public > Gio hang
- CheckoutController: @group Checkout

Scribe annotation format (v5.9):
- Class-level: @group GroupName\n\nGroup description
- Method: @bodyParam name type required|optional Description. Example: value
- Method: @queryParam name type Description. Example: value
- Method: @response STATUS_CODE {json}
- Method: @unauthenticated (for public endpoints)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize and complete Scribe docblocks on all controllers</name>
  <files>
    app/Presentation/Http/Controllers/Auth/AuthController.php,
    app/Presentation/Http/Controllers/Admin/CategoryController.php,
    app/Presentation/Http/Controllers/Admin/ProductController.php,
    app/Presentation/Http/Controllers/Admin/StockAdjustmentController.php,
    app/Presentation/Http/Controllers/Admin/ProductImageController.php,
    app/Presentation/Http/Controllers/Admin/OrderController.php,
    app/Presentation/Http/Controllers/Admin/DashboardController.php,
    app/Presentation/Http/Controllers/Public/ProductController.php,
    app/Presentation/Http/Controllers/Public/CartController.php,
    app/Presentation/Http/Controllers/Public/CheckoutController.php,
    config/scribe.php
  </files>

  <read_first>
    app/Presentation/Http/Controllers/Auth/AuthController.php,
    app/Presentation/Http/Controllers/Admin/CategoryController.php,
    app/Presentation/Http/Controllers/Admin/ProductController.php,
    app/Presentation/Http/Controllers/Admin/StockAdjustmentController.php,
    app/Presentation/Http/Controllers/Admin/ProductImageController.php,
    app/Presentation/Http/Controllers/Admin/OrderController.php,
    app/Presentation/Http/Controllers/Admin/DashboardController.php,
    app/Presentation/Http/Controllers/Public/ProductController.php,
    app/Presentation/Http/Controllers/Public/CartController.php,
    app/Presentation/Http/Controllers/Public/CheckoutController.php,
    config/scribe.php,
    app/Presentation/Http/Requests/AdminPlaceOrderRequest.php,
    app/Presentation/Http/Requests/UpdateOrderStatusRequest.php,
    app/Presentation/Http/Requests/UpdateDeliveryMethodRequest.php,
    routes/api.php
  </read_first>

  <action>
    Per D-07: cover ALL v1 endpoints. Per D-08: all descriptions in Vietnamese. Per D-09: Scribe v5.9 already installed, just needs annotations.

    **Step 1: Standardize @group names across all controllers**

    Use consistent `>` separator (not `-`). Vietnamese for group descriptions.

    Target groups (in display order):
    1. `Auth` — Xac thuc admin
    2. `Admin > Dashboard` — Tong quan don hang
    3. `Admin > Don hang` — Quan ly don hang (CHANGE from "Admin - Orders")
    4. `Admin > San pham` — Quan ly san pham (keep)
    5. `Admin > Danh muc` — Quan ly danh muc (keep)
    6. `Admin > Ton kho` — Dieu chinh ton kho (keep)
    7. `Admin > Anh san pham` — Quan ly anh san pham (keep)
    8. `Public > San pham` — Xem san pham (keep)
    9. `Public > Gio hang` — Gio hang khach (keep)
    10. `Public > Checkout` — Dat hang (CHANGE from just "Checkout")

    **Step 2: For each controller, ensure every method has:**

    a. Vietnamese endpoint title (first line of docblock)
    b. Vietnamese description paragraph
    c. `@bodyParam` for every POST/PATCH body field with Vietnamese description and `Example:` value
    d. `@queryParam` for every GET query parameter
    e. `@response` for POST/PATCH/DELETE endpoints (Scribe ResponseCalls only auto-runs GET)
    f. `@unauthenticated` on public endpoints (Public ProductController, CartController, CheckoutController)

    Specific annotations needed per controller:

    **AuthController** (`@group Auth`):
    - `login()`: @bodyParam email, password; @response 200 with token; @response 422 validation; @unauthenticated
    - `logout()`: @response 200 with message

    **CategoryController** (`@group Admin > Danh muc`):
    - Verify existing annotations are complete. Add @response for store/update/destroy if missing.

    **ProductController Admin** (`@group Admin > San pham`):
    - Verify all CRUD methods have @bodyParam and @response annotations.
    - `toggleActive()`: needs @response if missing

    **StockAdjustmentController** (`@group Admin > Ton kho`):
    - `index()`: @queryParam for any params; @response 200
    - `store()`: @bodyParam for adjustment fields; @response 201

    **ProductImageController** (`@group Admin > Anh san pham`):
    - `store()`: @bodyParam image file; @response 201
    - `setPrimary()`: @response 200
    - `destroy()`: @response 200

    **OrderController** (CHANGE to `@group Admin > Don hang`):
    - `index()`: already annotated from Plan 01
    - `store()`: already has annotations, verify completeness
    - `show()`: verify
    - `updateStatus()`: verify
    - `cancel()`: verify
    - `confirmPayment()`: verify
    - `updateDeliveryMethod()`: verify

    **DashboardController** (`@group Admin > Dashboard`):
    - Already annotated from Plan 01, verify

    **Public ProductController** (`@group Public > San pham`):
    - `index()`: @unauthenticated, @queryParam for filters, @response 200
    - `show()`: @unauthenticated, @response 200

    **CartController** (`@group Public > Gio hang`):
    - All methods: @unauthenticated
    - `store()`: @response 201 with cart token
    - `show()`: @response 200
    - `addItem()`: @bodyParam, @response 200
    - `updateItem()`: @bodyParam, @response 200
    - `removeItem()`: @response 200

    **CheckoutController** (CHANGE to `@group Public > Checkout`):
    - `store()`: @unauthenticated, @bodyParam for all checkout fields, @response 201

    **Step 3: Update config/scribe.php**

    Add group ordering to `config/scribe.php`. Find the `'groups'` key (or add it if not present) and set:
    ```php
    'groups' => [
        'order' => [
            'Auth',
            'Admin > Dashboard',
            'Admin > Don hang',
            'Admin > San pham',
            'Admin > Danh muc',
            'Admin > Ton kho',
            'Admin > Anh san pham',
            'Public > San pham',
            'Public > Gio hang',
            'Public > Checkout',
        ],
    ],
    ```

    **Step 4: Generate and validate**

    Run `php artisan scribe:generate` — must exit 0.
    Verify `public/docs/index.html` exists.
    Run `php artisan test` to ensure no regressions from docblock changes.
    Run `php -d memory_limit=512M vendor/bin/phpstan analyse` to ensure no type errors.
  </action>

  <verify>
    <automated>cd /Users/toney/projects/ban-chim-bo-cau && php artisan scribe:generate && test -f public/docs/index.html && php artisan test && php -d memory_limit=512M vendor/bin/phpstan analyse</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan scribe:generate` exits 0
    - public/docs/index.html exists and is non-empty
    - OrderController.php contains `@group Admin > Don hang` (not `Admin - Orders`)
    - CheckoutController.php contains `@group Public > Checkout`
    - Public ProductController contains `@unauthenticated`
    - CartController.php contains `@unauthenticated`
    - CheckoutController.php contains `@unauthenticated`
    - config/scribe.php contains `'order'` with group list array
    - All POST/PATCH/DELETE controller methods have `@response` annotation (grep for `@response` in each controller)
    - `php artisan test` exits 0
    - `php -d memory_limit=512M vendor/bin/phpstan analyse` exits 0
  </acceptance_criteria>

  <done>
    All v1 controllers have complete Scribe docblocks with Vietnamese descriptions. Group names standardized. Public endpoints marked @unauthenticated. config/scribe.php has group ordering.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify generated API documentation</name>
  <files>public/docs/index.html</files>
  <action>
    Human visually verifies the generated API documentation is complete and accurate.

    What was built: Complete Scribe API documentation covering all v1 endpoints (Auth, Dashboard, Orders, Products, Categories, Inventory, Cart, Checkout) with Vietnamese descriptions, example requests/responses, and logical grouping.

    How to verify:
    1. Start dev server: `php artisan serve`
    2. Open browser: http://localhost:8000/docs
    3. Verify all groups appear in sidebar: Auth, Admin > Dashboard, Admin > Don hang, Admin > San pham, Admin > Danh muc, Admin > Ton kho, Admin > Anh san pham, Public > San pham, Public > Gio hang, Public > Checkout
    4. Check Auth group: login/logout endpoints present with request/response examples
    5. Check Admin > Dashboard: dashboard endpoint with response showing orders_by_status
    6. Check Admin > Don hang: all order endpoints (list, create, show, update status, cancel, confirm payment, delivery method)
    7. Check Public endpoints: marked as not requiring authentication
    8. Verify descriptions are in Vietnamese
    9. Verify example values make sense (Vietnamese names, phone numbers starting with 0, realistic data)

    Resume signal: Type "approved" if docs look complete and accurate, or describe issues to fix.
  </action>
  <verify>
    <automated>test -f /Users/toney/projects/ban-chim-bo-cau/public/docs/index.html</automated>
  </verify>
  <done>Human confirms API documentation is complete, accurate, and browsable with all v1 endpoints.</done>
</task>

</tasks>

<verification>
1. `php artisan scribe:generate` exits 0
2. `public/docs/index.html` exists and is browsable
3. `php artisan test` — full suite green
4. `php -d memory_limit=512M vendor/bin/phpstan analyse` — zero errors
5. Human verification that docs are complete and accurate
</verification>

<success_criteria>
- Running `php artisan scribe:generate` produces a browsable API reference covering all v1 endpoints with accurate request/response examples (ADMN-04, TECH-05)
- All endpoint descriptions and parameter descriptions are in Vietnamese (D-08)
- Generated docs available at /docs with logical group ordering
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-operations-docs/04-02-SUMMARY.md`
</output>
